isDebug: true
dadaBaseUrl: localhost
dataBaseParam: useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&zeroDateTimeBehavior=convertToNull&allowPublicKeyRetrieval=true
fileSavePath: D:/temp/crabapples/
spring:
  cloud:
    nacos:
      config:
        enabled: false
        file-extension: yml
        server-addr: 192.168.3.20:8848
      discovery:
        server-addr: 192.168.3.20:8848
  datasource:
    driverClassName: ${dataBaseDriveV8}
    url: jdbc:mysql://${dadaBaseUrl}:3306/wechat_official?useSSL=false&${dataBaseParam}
    password: root
    username: root
  redis:
    database: 0
    host: 192.168.3.20
    port: 6379
    password: crabapples
#logging
logging:
  level:
    cn:
      crabapples: debug
  pattern:
    level: info
  file:
    name: d:/logs/crabapples/${spring.application.name}